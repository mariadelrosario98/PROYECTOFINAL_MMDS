# ==========================================================
# 1. ENTORNO VIRTUAL DE PYTHON
# ==========================================================
# Excluir el entorno virtual (dependiendo de si se llama venv o .venv)
venv/
.venv/
# Archivos de compilación de Python
__pycache__/
*.pyc

# ==========================================================
# 2. INFRAESTRUCTURA COMO CÓDIGO (TERRAFORM)
# ==========================================================
# DIRECTORIO RAIZ DE TERRAFORM (o donde se ejecuta terraform init)
.terraform/

# Archivos de estado (CRÍTICO: No deben subirse nunca)
*.tfstate
*.tfstate.*
crash.log
output.tf

# Archivos de variables sensibles (Contienen credenciales o valores secretos)
# Esto excluye el archivo que mostraste:
terraform.tfvars
*.tfvars.json

# ==========================================================
# 3. DATOS GRANDES Y LOGS
# ==========================================================
# Excluir el archivo de datos grande de LiveJournal
DATA/soc-LiveJournal1.txt
# Si el directorio DATA/ solo contiene archivos grandes, exclúyelo todo:
DATA/